{
  "name": "@terminus-os/systemd",
  "version": "0.1.0",
  "description": "sd_notify(3) for Bun daemons: the readiness datagram that completes a Type=notify start job, so a restart returns on the daemon's own serving edge rather than on fork.",
  "type": "module",
  "private": true,
  "module": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  }
}
