{
  "name": "@terminus-os/tx",
  "version": "0.1.0",
  "description": "tx — extensible operator CLI for the txd estate daemon",
  "type": "module",
  "private": true,
  "bin": { "tx": "src/main.ts" },
  "dependencies": {
    "@terminus-os/contracts": "workspace:*",
    "@terminus-os/txd": "workspace:*",
    "@tokenamby-code/stc-contract": "1.3.0"
  },
  "scripts": { "test": "bun test", "typecheck": "tsc --noEmit" },
  "devDependencies": { "@types/bun": "^1.3.14", "typescript": "^5.8.0" }
}
