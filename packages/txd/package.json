{
  "name": "@terminus-os/txd",
  "version": "0.1.0",
  "description": "txd — the estate daemon for k12 boxes: authoritative, event-sourced tmux control plane (Bun/TypeScript). tmuxctld successor; tx is its rote auxiliary client.",
  "type": "module",
  "private": true,
  "exports": { "./identity": "./src/identity.ts" },
  "scripts": {
    "start": "bun src/daemon.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@terminus-os/contracts": "workspace:*",
    "@terminus-os/db": "workspace:*",
    "@tokenamby-code/agent-contract": "1.2.0",
    "@tokenamby-code/stc-contract": "1.6.4"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.8.0"
  }
}
