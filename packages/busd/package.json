{
  "name": "@terminus-os/busd",
  "version": "0.1.0",
  "description": "busd — the central event bus for k12 boxes: append-only Postgres journal, subscription config, per-subscriber cursored HTTP fan-out (transactional outbox; Bun/TypeScript).",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun src/daemon.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@terminus-os/contracts": "workspace:*",
    "@terminus-os/db": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.8.0"
  }
}
