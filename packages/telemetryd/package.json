{
  "name": "@terminus-os/telemetryd",
  "version": "0.1.0",
  "description": "Typed passive telemetry ingress for desktop and phone observations.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun src/daemon.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tokenamby-code/stc-contract": "1.0.0",
    "@terminus-os/contracts": "workspace:*",
    "@terminus-os/db": "workspace:*"
  }
}
