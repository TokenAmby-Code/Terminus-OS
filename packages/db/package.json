{
  "name": "@terminus-os/db",
  "version": "0.0.0",
  "description": "PostgreSQL 18 interaction foundation for Terminus-OS. Typed endpoint config (peer-auth unix sockets, remote/local earmarks), fail-loud client, Zod-validated query boundary, forward-only migrations, health contract.",
  "type": "module",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./config": "./src/config.ts",
    "./client": "./src/client.ts",
    "./migrate": "./src/migrate.ts",
    "./health": "./src/health.ts"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
